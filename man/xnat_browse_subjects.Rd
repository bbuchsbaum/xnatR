% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/browse.R
\name{xnat_browse_subjects}
\alias{xnat_browse_subjects}
\title{Browse subjects interactively}
\usage{
xnat_browse_subjects(
  project_id = NULL,
  client = NULL,
  pattern = NULL,
  columns = c("ID", "label", "gender", "age"),
  page_size = 20,
  .interactive = interactive()
)
}
\arguments{
\item{project_id}{Project identifier (optional in interactive mode).}

\item{client}{Optional \code{xnat_client}. If \code{NULL}, uses the global session.}

\item{pattern}{Optional substring filter applied to the current page.}

\item{columns}{Columns to display (defaults to common subject fields).}

\item{page_size}{Rows per page (default 20).}

\item{.interactive}{If \code{FALSE}, returns the listing tibble without prompting.}
}
\value{
A tibble row selection (or \code{NULL} if quit). In non-interactive mode,
returns a tibble of subjects.
}
\description{
If \code{project_id} is omitted in interactive mode, the function first prompts for
a project.
}
