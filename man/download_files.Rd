% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download_files}
\alias{download_files}
\title{Download Files from XNAT}
\usage{
download_files(
  project_id,
  subject_id,
  experiment_id,
  scan_id = "ALL",
  resource = NULL,
  format = "zip",
  dest_dir = getwd(),
  dest_file = NULL,
  progress = TRUE,
  client = NULL
)
}
\arguments{
\item{project_id}{The project identifier.}

\item{subject_id}{The subject identifier.}

\item{experiment_id}{The experiment identifier.}

\item{scan_id}{Scan ID(s) to download. Can be:
\itemize{
\item A single scan ID (e.g., "1")
\item Multiple IDs (e.g., c("1", "2", "3") or "1,2,3")
\item "ALL" to download all scans (default)
}}

\item{resource}{Resource name (e.g., "DICOM", "NIFTI"). NULL for all resources.}

\item{format}{Download format: "zip" (default) or "tar.gz".}

\item{dest_dir}{Destination directory. Defaults to current working directory.}

\item{dest_file}{Custom destination filename. If NULL, auto-generated.}

\item{progress}{Show download progress bar. Default TRUE.}

\item{client}{Optional \code{xnat_client}. If \code{NULL}, uses the global session.}
}
\value{
Invisibly returns the path to the downloaded file.
}
\description{
Downloads files from an XNAT experiment, with options to select specific
scans and resources.
}
\examples{
\dontrun{
authenticate_xnat(base_url = "https://central.xnat.org",
                  username = "guest", password = "guest")

# Download all scans as ZIP
download_files(
  project_id = "MyProject",
  subject_id = "Subject001",
  experiment_id = "Exp001"
)

# Download specific scans
download_files(
  project_id = "MyProject",
  subject_id = "Subject001",
  experiment_id = "Exp001",
  scan_id = c("1", "2")
)

# Download DICOM only
download_files(
  project_id = "MyProject",
  subject_id = "Subject001",
  experiment_id = "Exp001",
  resource = "DICOM"
)
}

}
