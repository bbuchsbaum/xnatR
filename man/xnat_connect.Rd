% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{xnat_connect}
\alias{xnat_connect}
\title{XNAT client objects}
\usage{
xnat_connect(
  base_url = NULL,
  username = NULL,
  password = NULL,
  ssl_verify = TRUE,
  verify = TRUE,
  use_jsession = FALSE,
  jsession = NULL,
  xnat_name = NULL
)
}
\arguments{
\item{base_url}{Base URL of the XNAT server (e.g., \code{"https://central.xnat.org"}).}

\item{username}{Username, or \code{NULL} if using \code{jsession} only.}

\item{password}{Password, or \code{NULL} if using \code{jsession} only.}

\item{ssl_verify}{Whether to verify SSL certificates. Default \code{TRUE}.}

\item{verify}{Whether to verify credentials by making a test request. Default \code{TRUE}.}

\item{use_jsession}{Whether to establish a JSESSION cookie-based session instead
of using Basic Auth for each request. Default \code{FALSE}.}

\item{jsession}{Optional existing JSESSIONID value. When provided, requests use
cookie auth and do not send Basic Auth.}

\item{xnat_name}{Optional server nickname used for compatibility with Rxnat-style
environment variables (\verb{<XNAT_NAME>_RXNAT_USER}, \verb{<XNAT_NAME>_RXNAT_PASS}).}
}
\value{
\code{xnat_connect()} returns an \code{xnat_client}.
}
\description{
\code{xnat_connect()} creates an explicit client object that can be passed to all
xnatR functions via the \code{client} argument. This avoids relying on global
session state stored in the package environment.
}
\details{
\code{authenticate_xnat()} remains available for a global-session workflow.
}
\examples{
\dontrun{
client <- xnat_connect(
  base_url = "https://central.xnat.org",
  username = "guest",
  password = "guest",
  use_jsession = TRUE
)

projects <- list_projects(client = client)
}
}
