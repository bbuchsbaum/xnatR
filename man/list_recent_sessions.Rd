% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list-experiments.R
\name{list_recent_sessions}
\alias{list_recent_sessions}
\title{List Recent Sessions for a Project}
\usage{
list_recent_sessions(
  project_id,
  n = NULL,
  subject_id = NULL,
  columns = NULL,
  client = NULL
)
}
\arguments{
\item{project_id}{The project identifier (required).}

\item{n}{Maximum number of sessions to return. \code{NULL} (the default)
returns all sessions.}

\item{subject_id}{Optional subject identifier to scope results.}

\item{columns}{Character vector of column names to include.}

\item{client}{Optional \code{xnat_client}. If \code{NULL}, uses the global session.}
}
\value{
A tibble of class \code{xnat_experiments}, ordered most-recent first.
}
\description{
Convenience wrapper around \code{\link[=list_experiments]{list_experiments()}} that returns experiments
sorted by date (most recent first) with an optional cap on the number of
rows returned.
}
\examples{
\dontrun{
# 10 most recent sessions in a project
recent <- list_recent_sessions("MyProject", n = 10)

# All sessions for a subject, newest first
recent_subj <- list_recent_sessions("MyProject", subject_id = "S001")
}

}
